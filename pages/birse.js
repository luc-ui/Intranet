import Publis from '../components/Publis'

export default function Birse({ selectStrass }) {
	const authStrass = ['AE','cvis']

	return <Publis type="birse" name="Birse - Bien-être" authStrass={authStrass} selectStrass={selectStrass}/>
}
