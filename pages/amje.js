import Publis from '../components/Publis'

export default function Amje({ selectStrass }) {
	const authStrass = ['AE','cvis']

	return <Publis type="amje" name="Arts&Métiers - Junior Entreprise" authStrass={authStrass} selectStrass={selectStrass}/>
}
